all:
  children:
    nfvis:
      # hosts:           
      #   dut:
      children:
        encs:
          hosts:
            harness:
    dut_host:
      hosts:
        dut:
    vnf:
      hosts:
        host1:
        # host2:
        # nat-rtr:
        control:
          ansible_python_interpreter: /usr/bin/python3
    test_host:
      hosts:
        host1:
        # host2:
    control_host:
      hosts:
        control:
