all:
  children:
    nfvis:
      # hosts:           
      #   dut:
      children:
        encs:
          hosts:
            harness:
    dut_host:
      hosts:
        dut:
    vnf:
      hosts:
        hostlan1:
        hostwan1:
        control:
          #ansible_python_interpreter: /usr/bin/python3
    test_host:
      hosts:
        hostlan1:
        hostwan1:
    control_host:
      hosts:
        control:
